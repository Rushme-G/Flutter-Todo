import 'package:flutter/material.dart';

class BottomNavTravel extends StatelessWidget {
  const BottomNavTravel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
